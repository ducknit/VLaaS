# VLaaS Backend - Example environment variables
# Copy this file to `.env` and fill in real values before running.
# DO NOT commit your real `.env` to source control.

########################
# Server
########################
PORT=4000
NODE_ENV=development

########################
# Google OAuth (Passport)
# - Create credentials in Google Cloud Console
# - Set Authorized redirect URI to: http://localhost:4000/api/auth/google/callback
########################
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:4000/api/auth/google/callback

########################
# AWS / DynamoDB
# - For local dev you can use AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY
# - For production prefer IAM roles (ECS task role / EC2 role / Lambda role)
########################
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY

# DynamoDB table names
DYNAMO_TABLE_USERS=VLAAS_Users
DYNAMO_TABLE_SESSIONS=VLAAS_Sessions
# (Optional) add more tables for labs, audit logs, etc.

########################
# JWT
########################
JWT_SECRET=replace_with_a_strong_random_secret
JWT_EXPIRES_IN=7d

########################
# Frontend (used as OAuth redirect target after success)
########################
FRONTEND_URL=http://localhost:3000

########################
# Optional / Orchestration (fill if you integrate ECS / ECR / other AWS services)
########################
AWS_ECS_CLUSTER=your-ecs-cluster
AWS_ECS_TASK_DEFINITION=your-task-def
ECR_REPOSITORY=your-ecr-repo

########################
# Misc
########################
# Set to 'true' to enable verbose logging in app (not for production)
VERBOSE_LOGGING=true

# If you use AWS profiles / shared config, you can optionally specify a profile
# AWS_PROFILE=default

# Notes:
#  - Never store production AWS keys in plain text or in git. Use IAM roles, Secrets Manager, or Parameter Store.
#  - Use a long, randomly generated value for JWT_SECRET.
#  - In production set NODE_ENV=production and use HTTPS for callbacks.
